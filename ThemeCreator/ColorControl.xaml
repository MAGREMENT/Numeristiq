<UserControl x:Class="ThemeCreator.ColorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ThemeCreator"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Border Grid.Column="0" Width="40" Height="40" Margin="5" x:Name="Color"
                BorderBrush="{DynamicResource Text}" BorderThickness="1"/>
        <TextBlock Grid.Column="1" Foreground="{DynamicResource Text}"
                   VerticalAlignment="Center" FontSize="16" x:Name="ColorName"/>
    </Grid>
</UserControl>
