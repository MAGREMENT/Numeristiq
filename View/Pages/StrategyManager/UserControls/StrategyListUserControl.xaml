<UserControl x:Class="View.Pages.StrategyManager.UserControls.StrategyListUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <Border BorderBrush="LightGray" BorderThickness="0, 0, 0, 1" Margin="0, 0, 0, 5">
            <Grid Margin="0, 0, 0, 5" Height="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"></ColumnDefinition>
                    <ColumnDefinition Width="30"></ColumnDefinition>
                </Grid.ColumnDefinitions>
            
                <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="20">Strategies</TextBlock>
                <Image Grid.Column="1" x:Name="Trash">
                    <Image.Source>
                        <BitmapImage UriSource="../../../Images/bin.png"/>
                    </Image.Source>
                </Image>
            </Grid>
        </Border>
        <ScrollViewer Width="250" Height="450">
            <StackPanel Width="250" MinHeight="450" x:Name="List"/>
        </ScrollViewer>
    </StackPanel>
</UserControl>
