<UserControl x:Class="View.Pages.Solver.UserControls.StrategyListUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:utils="clr-namespace:View.Utility"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <Border BorderThickness="0,0,0,1" BorderBrush="DarkGray">
            <TextBlock FontSize="20" Margin="3">Strategies</TextBlock>
        </Border>
        <ScrollViewer Height="620" Background="{x:Static utils:ColorManager.Background1}">
            <StackPanel MinHeight="620" Width="180" x:Name="List" Orientation="Vertical"/>
        </ScrollViewer>
    </StackPanel>
</UserControl>
