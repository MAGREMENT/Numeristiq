<Window x:Class="View.FirstWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="FirstWindow" Height="200" Width="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" FontSize="30" FontWeight="SemiBold" Background="{DynamicResource Background2}"
                Margin="10" Click="GoToSudoku">Sudoku</Button>
        <Button Grid.Column="1" FontSize="30" FontWeight="SemiBold" Background="{DynamicResource Background2}"
                Margin="10" Click="GoToTectonic">Tectonic</Button>
    </Grid>
</Window>
