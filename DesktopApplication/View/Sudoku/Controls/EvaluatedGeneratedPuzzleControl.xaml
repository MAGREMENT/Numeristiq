<UserControl x:Class="DesktopApplication.View.Sudoku.Controls.EvaluatedGeneratedPuzzleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="{DynamicResource Background2}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Column="0" x:Name="Sudoku" Margin="5" FontSize="14" Foreground="{DynamicResource Text}"/>
        <TextBlock Grid.Column="1" x:Name="Rating" Margin="5" FontSize="14" Foreground="{DynamicResource Text}"/>
        <TextBlock Grid.Column="2" x:Name="Hardest" Margin="5" FontSize="14" />
    </Grid>
</UserControl>
