<UserControl x:Class="SudokuSolver.LiveModificationUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SudokuSolver"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Width="350" Height="150" Margin="0, 20, 0, 0" Orientation="Horizontal">
        <local:NumbersUserControl x:Name="Numbers" KeyDown="KeyPressed"/>
        <Grid Width="200" Height="150">
            <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                <RadioButton IsChecked="True" x:Name="A" >Change definitive number</RadioButton>
                <RadioButton x:Name="B">Remove possibilities</RadioButton>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
